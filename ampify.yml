version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm run build
    build:
      commands: []
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []